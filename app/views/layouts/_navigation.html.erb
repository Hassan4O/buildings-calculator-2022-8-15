<nav class="navbar navbar-expand-lg bg-light">
    <div class="container">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="true" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <% if current_user %>
                <%= link_to current_user.first_name , buildings_path, class: 'navbar-brand' %>
        <% else %>
            <%= link_to "Buildings Calculator ", root_path, class: 'navbar-brand' %>
        <% end %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
            <%= link_to 'Home', root_path, class: 'nav-link' %>
            </li>
            
            <% if current_user %>
            <li class="nav-item">
                <%= link_to 'Your Buildings', buildings_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
                <%= link_to 'New', new_building_path, class: 'nav-link' %>
            </li>
            <% end %>
        </ul>
            <% if current_user %>
                <%= link_to 'Sign out',  destroy_user_session_path, class: 'btn btn-outline-danger', data: { "turbo-method": :delete }%>
            <% end %>
        </div>
    </div>
    </div>
</nav>
